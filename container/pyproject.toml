[project]
name = "cars-mcp-server"
version = "0.1.0"
description = "MCP server exposing read-only tools over a sample car database"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.9.0",
]
